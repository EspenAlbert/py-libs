[project]
name = "zero-3rdparty"
version = "1.0.0+rc1"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = [
  "zero_3rdparty/*.py",
]
exclude = [
  "*_test.py",
  "conftest.py",
  "test_*.json",
]
